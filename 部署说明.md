# 儿童医院智能食谱 PWA 手动部署说明

## 发布内容

请将 `site` 文件夹内的全部内容上传到 GitHub 仓库根目录。不要只上传 `site` 文件夹本身。

本发布包为纯前端本地演示版：不连接 MySQL、云函数、COS 或真实 DeepSeek API。演示数据保存在当前设备的浏览器本地存储中，不同设备不会同步。

## GitHub Pages 设置

1. 新建一个 GitHub 仓库。
2. 将 `site` 内全部文件上传至仓库默认分支的根目录。
3. 打开仓库的 **Settings → Pages**。
4. 在 **Build and deployment** 中选择 **Deploy from a branch**。
5. Branch 选择默认分支（通常是 `main`），目录选择 `/(root)`，然后保存。
6. 等待 GitHub 生成 HTTPS 访问地址。

## 安装到手机

- Android：使用 Chrome 打开链接，选择“添加到主屏幕”或“安装应用”。
- iPhone：使用 Safari 打开链接，点击分享按钮，再选择“添加到主屏幕”。

## 演示数据说明

- 数据只保存在当前浏览器或当前安装的 PWA 中。
- 清理浏览器网站数据、卸载应用或更换设备可能导致数据丢失。
- 院方资料上传目前只演示选择和校验流程，不会上传到服务器。
- AI 内容目前为演示结果，不会调用真实模型。

## 后续重新生成

在 `taro-app` 目录执行 `npm run build:h5`，新的静态文件会生成到 `taro-app/dist/h5`。
